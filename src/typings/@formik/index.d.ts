/// <reference path="../_references.ts" />

type FormikProps = import("formik").FormikProps;
type FormikValues = import("formik").FormikValues;

declare type FormikProp = {
  formik: FormikProps<FormikValues>;
};
